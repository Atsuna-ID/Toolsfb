# Install Library
$ apt update && apt upgrade
$ pkg install python
$ pkg install git
$ pip install fake-email
$ pip install getpass
$ git clone https://github.com/Atsuna-ID/Toolsfb
cd Toolsfb
python tools.py
